# Path to the training and validation datasets
train: "C:/Users/river/488/FindDendrites/Images/Train"  # e.g., "C:/datasets/dendrites/train"
val: "C:/Users/river/488/FindDendrites/Images/Val"      # e.g., "C:/datasets/dendrites/val"

nc: 1

# List of class names (ensure the indices match your annotations)
names: [ "Dendrite" ]
